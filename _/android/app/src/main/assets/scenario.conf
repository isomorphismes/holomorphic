# Normal interactive explorer. Video and experiment branches should select motion
# by changing this data rather than rewriting the native engine or shader.
name=interactive
presentation=interactive
motion=none
field_speed=0.30
